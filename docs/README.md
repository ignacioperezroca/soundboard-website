# Soundboard Website project notes

## Purpose
Preserve the soundboard website in a maintainable static structure.

## Structure
- `index.html` for the public entry point
- `vercel.json` for deployment settings
- `package.json` for local scripts and builds

## Maintenance
- Keep source and content in the root site tree.
- Keep operational notes here in `docs/`.
